# Tự động thêm nguồn dữ liệu Prometheus vào Grafana (không phải cấu hình tay)
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true
